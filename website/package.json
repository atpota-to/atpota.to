{
  "name": "atpotato-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "serve .",
    "build": "mkdir -p public && cp -r index.html styles.css theme.js logo-hover.js guides public/ && (cp -r *.png *.jpg *.JPG public/ 2>/dev/null || true)",
    "start": "serve public"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
} 